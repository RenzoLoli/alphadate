---
interface Props {
  photo: string;
  className?: string;
}

const { photo, className } = Astro.props;
---

<img
  class:list={["flex flex-row text-center rounded-full bg-pink-800 size-[60px] p-2", className]}
  src={photo}
  alt="user photo"
/>
